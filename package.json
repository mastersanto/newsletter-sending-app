{
  "name": "newsletter-sending-app",
  "packageManager": "yarn@4.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --topological-dev -pi run build",
    "dev-api": "yarn workspace api dev",
    "start-app": "yarn workspace app start"
  }
}
